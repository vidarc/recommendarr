/** Single source of truth for e2e test credentials — must match the setup in auth.setup.ts. */
const adminUsername = "admin";
const adminPassword = "admin1234";
const storageStatePath = "e2e/.auth/user.json";

export { adminPassword, adminUsername, storageStatePath };
